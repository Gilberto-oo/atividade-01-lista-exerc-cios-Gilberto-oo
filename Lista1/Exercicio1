/*1. Faça um programa que recebe o número de horas trabalhadas, e o valor da hora trabalhada. Calcule e mostre o salário a receber seguindo estas regras:
    • o salário bruto equivale ao número de horas trabalhadas multiplicado pelo valor da
    hora trabalhada;
    • o imposto equivale a 30% do salário bruto;
    • o salário a receber equivale ao salário bruto menos o imposto.*/
import javax.swing.JOptionPane;
public class Lista1Exe1 
{
    public static void main(String[] args) 
    {
        double imposto=0, salTotal=0,salBruto=0, horas, valHoras;
        String hora, valHora;
        
        hora = JOptionPane.showInputDialog("Informe a quantidade de horas trabalhadas: ");
        horas = Float.parseFloat(hora);

        valHora = JOptionPane.showInputDialog("Informe o valor ganhado por hora: ");
        valHoras = Float.parseFloat(valHora);

        salBruto = horas*valHoras;
        imposto = (0.3)*salBruto;
        salTotal = salBruto-imposto;

        JOptionPane.showMessageDialog(null,"O salário bruto é de: "+salBruto+" R$\nO valor do imposto é de: "+imposto+" R$\nO valor do salário total é de: "+salTotal+" R$");
    }
}
